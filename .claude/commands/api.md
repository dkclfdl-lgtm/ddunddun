Next.js API Route를 생성합니다.

$ARGUMENTS 에 대한 API Route를 생성해주세요.

규칙:
1. src/app/api/ 디렉토리에 route.ts 파일 생성
2. Supabase 서버 클라이언트 사용
3. 적절한 HTTP 메서드 핸들러 (GET, POST, PUT, DELETE)
4. zod로 요청 바디 유효성 검증
5. 에러 핸들링 (try-catch, 적절한 HTTP 상태 코드)
6. TypeScript 타입 안전성 확보
7. .claude/docs/PRD.md의 API 설계를 참고
