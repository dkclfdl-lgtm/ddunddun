프로젝트 초기 설정을 진행합니다.

다음 순서로 초기 설정을 진행해주세요:

1. **필요 패키지 설치 명령어 출력** (사용자가 직접 실행)
   - recharts, @tanstack/react-table, react-day-picker, cmdk, sonner
   - @hello-pangea/dnd, react-hotkeys-hook, numbro
   - @supabase/supabase-js, @supabase/ssr
   - prettier, husky, lint-staged (dev)

2. **shadcn/ui 컴포넌트 설치 명령어 출력** (사용자가 직접 실행)
   - table, tabs, progress, calendar, popover, command
   - navigation-menu, scroll-area, skeleton, switch, tooltip, alert, collapsible

3. **디렉토리 구조 생성**
   - src/components/layout/
   - src/components/charts/
   - src/constants/
   - src/types/
   - src/lib/supabase/
   - src/remote/
   - supabase/migrations/

4. **기본 파일 생성**
   - Supabase 클라이언트 설정
   - 공통 타입 정의
   - 공통 상수 (컬러, 라우트 등)
   - 공통 레이아웃 (사이드바, 탑바)

5. **Pretendard 폰트 설정**

6. **글로벌 CSS 업데이트** (네이버 스타일 컬러 변수)
