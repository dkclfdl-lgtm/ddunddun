diff --git a/node_modules/@radix-ui/react-compose-refs/dist/index.js b/node_modules/@radix-ui/react-compose-refs/dist/index.js
index 5ba7a95..168bcd6 100644
--- a/node_modules/@radix-ui/react-compose-refs/dist/index.js
+++ b/node_modules/@radix-ui/react-compose-refs/dist/index.js
@@ -39,36 +39,45 @@ module.exports = __toCommonJS(index_exports);
 var React = __toESM(require("react"));
 function setRef(ref, value) {
   if (typeof ref === "function") {
-    return ref(value);
+    const cleanup = ref(value);
+    if (typeof cleanup === "function") {
+      return cleanup;
+    }
+    return;
   } else if (ref !== null && ref !== void 0) {
     ref.current = value;
   }
 }
 function composeRefs(...refs) {
   return (node) => {
-    let hasCleanup = false;
-    const cleanups = refs.map((ref) => {
-      const cleanup = setRef(ref, node);
-      if (!hasCleanup && typeof cleanup == "function") {
-        hasCleanup = true;
+    const cleanups = [];
+    for (let i = 0; i < refs.length; i++) {
+      const cleanup = setRef(refs[i], node);
+      if (typeof cleanup === "function") {
+        cleanups.push(cleanup);
+      } else {
+        cleanups.push(refs[i]);
       }
-      return cleanup;
-    });
-    if (hasCleanup) {
-      return () => {
-        for (let i = 0; i < cleanups.length; i++) {
-          const cleanup = cleanups[i];
-          if (typeof cleanup == "function") {
-            cleanup();
-          } else {
-            setRef(refs[i], null);
-          }
-        }
-      };
     }
+    return () => {
+      for (let i = 0; i < cleanups.length; i++) {
+        const cleanup = cleanups[i];
+        if (typeof cleanup === "function" && cleanup !== refs[i]) {
+          cleanup();
+        } else {
+          setRef(typeof cleanup === "function" ? cleanup : refs[i], null);
+        }
+      }
+    };
   };
 }
 function useComposedRefs(...refs) {
-  return React.useCallback(composeRefs(...refs), refs);
+  // Store refs in a mutable ref to avoid re-creating the callback on every render
+  const refsRef = React.useRef(refs);
+  refsRef.current = refs;
+  // eslint-disable-next-line react-hooks/exhaustive-deps
+  return React.useCallback((node) => {
+    return composeRefs(...refsRef.current)(node);
+  }, []);
 }
 //# sourceMappingURL=index.js.map
diff --git a/node_modules/@radix-ui/react-compose-refs/dist/index.mjs b/node_modules/@radix-ui/react-compose-refs/dist/index.mjs
index 7dd9172..fc49189 100644
--- a/node_modules/@radix-ui/react-compose-refs/dist/index.mjs
+++ b/node_modules/@radix-ui/react-compose-refs/dist/index.mjs
@@ -32,7 +32,13 @@ function composeRefs(...refs) {
   };
 }
 function useComposedRefs(...refs) {
-  return React.useCallback(composeRefs(...refs), refs);
+  // Store refs in a mutable ref to avoid re-creating the callback on every render
+  const refsRef = React.useRef(refs);
+  refsRef.current = refs;
+  // eslint-disable-next-line react-hooks/exhaustive-deps
+  return React.useCallback((node) => {
+    return composeRefs(...refsRef.current)(node);
+  }, []);
 }
 export {
   composeRefs,
